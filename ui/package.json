{
  "name": "agent-hive-ui",
  "version": "0.2.0",
  "description": "Agent Hive Web UI — custom chat + GitHub panel",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.0.0"
  }
}
